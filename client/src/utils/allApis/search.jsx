const host = `https://context-api-2my7afm7yq-ue.a.run.app`
// const host = `https://backend.neptunestech.com:8049`
const api = `${host}/api`

const search_api = `${api}/search`

const SEARCH_APIS = {
    GET: {
        SEARCH: search_api
    }
}

export default SEARCH_APIS